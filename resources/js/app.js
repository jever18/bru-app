// resources/js/app.js

import './bootstrap';

// Impor Bootstrap JavaScript
import * as bootstrap from 'bootstrap';
window.bootstrap = bootstrap; // Opsional, tapi berguna

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
